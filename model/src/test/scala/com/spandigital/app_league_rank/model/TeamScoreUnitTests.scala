package com.spandigital.app_league_rank.model

import org.scalatest.funsuite.AnyFunSuite

class TeamScoreUnitTests extends AnyFunSuite {
  test("can handle team name with more than 2 words") {}

  test("can handle alpha numeric name") {
    
  }

  test("can handle name with number separted by space") {
    //exampe Team A 1 Awesome, 0
  }
  //more tests
}
